name: alterione_agent
description: Core daemon of the AlteriOne AI agent framework — orchestrates personas, LLM modules, memory, skills, and exposes an HTTP/WebSocket API.
version: 0.0.1
homepage: https://alteri.one
repository: https://github.com/AlteriOne/alterione-agent
issue_tracker: https://github.com/AlteriOne/alterione-agent/issues
documentation: https://alteri.one/docs
publish_to: none

topics:
  - ai
  - agent
  - llm
  - daemon
  - mcp

environment:
  sdk: ^3.12.0

dependencies:
  path: ^1.9.0
  alterione_sdk: #^0.1.0
    path: ../alterione_sdk

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
